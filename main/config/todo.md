⚙️ config
Description

The config component centralizes configuration values used by multiple components. It defines system parameters, constants, and compile-time or runtime configuration values.

This component should not contain logic.

Goals

Provide a single source of truth for configuration

Avoid hardcoded values inside components

Allow easy tuning of system behavior

Files

system_config.h

network_config.h

device_config.h